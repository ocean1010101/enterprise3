data/seedData.json holds the database

scripts/evaluateML.js is used to test the k-NN machine learning model

app/events/page.tsx contains the k-NN algorithm through the "useMemo" at the top of the code.

scripts/recommendedEvents.js contains the code for the k-NN algorithm

scripts/populated.js checks if my seedData.js (and the data inside it) exists and works